defaults: &defaults
  site_name: "Medusa"
  help_url: https://github.com/McNamara84/medusa-earthshape
  admin:
    email: medusa@example.com
    initial_password: vQxPIFMZ
  barcode:
    prefix:
    type: 2D
  backup:
    files:
      dir_path: "/tmp/medusa/backup/files"
    db:
      dir_path: "/tmp/medusa/backup/db"

development:
  <<: *defaults

test:
  <<: *defaults

production:
  <<: *defaults

staging:
  <<: *defaults

timemachine:
  <<: *defaults

backup:
  host: backup.example.com
  username: medusa
  dir_path:
    files: /tmp/medusa/backup/files
    db: /tmp/medusa/backup/db
