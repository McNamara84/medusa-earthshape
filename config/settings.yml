# Default settings for all environments
# Environment-specific overrides go in config/settings/<environment>.yml

site_name: Medusa
help_url: http://dream.misasa.okayama-u.ac.jp/documentation/

admin:
  email: medusa@example.com
  initial_password: changeme

barcode:
  prefix:
  type: 2D

backup:
  files:
    dir_path: "/tmp/medusa/backup/files"
  db:
    dir_path: "/tmp/medusa/backup/db"

# Root-level backup settings used by app/models/backup.rb
# These are accessed via Backup.host, Backup.username, Backup.dir_path
backup_server:
  host: backup.example.com
  username: medusa
  dir_path:
    files: /tmp/medusa/backup/files
    db: /tmp/medusa/backup/db
