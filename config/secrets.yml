# Be sure to restart your server when you modify this file.

# Your secret key is used for verifying the integrity of signed cookies.
# If you change this key, all old signed cookies will become invalid!

# Make sure the secret is at least 30 characters and all random,
# no regular words or you'll be exposed to dictionary attacks.
# You can use `rails secret` to generate a secure secret key.

# Make sure the secrets in this file are kept private
# if you're sharing your code publicly.

# Shared secrets are available across all environments.

# shared:
#   api_key: a1B2c3D4e5F6

# Environmental secrets are only available for that specific environment.

development:
  # Prefer setting SECRET_KEY_BASE in your environment.
  # Fallback is a non-sensitive development key (OK for local/dev only).
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") { "d3b07384d113edec49eaa6238ad5ff00c5e478d59288c841aa530db6845c4c75" } %>

test:
  # Prefer setting SECRET_KEY_BASE in your environment.
  # Fallback is a non-sensitive test key (OK for CI/local test only).
  secret_key_base: <%= ENV.fetch("SECRET_KEY_BASE") { "9b74c9897bac770ffc029102a200c5de5c2a7e0f3b2a0b822cd15d6c15b0f00a" } %>

# Do not keep production secrets in the unencrypted secrets file.
# Instead, either read values from the environment.
# Or, use `bin/rails secrets:setup` to configure encrypted secrets
# and move the `production:` environment over there.

production:
  secret_key_base: <%= ENV["SECRET_KEY_BASE"] %>
