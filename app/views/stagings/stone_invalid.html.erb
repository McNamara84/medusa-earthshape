<div class="alert alert-danger">
 <div><%= link_to :back, adjust_url_by_requesting_tab(safe_referer_url) %></div>
  Can not create sample.
  <% if local_assigns[:error].present? %>
    <br><br>
    <%= error %>
  <% end %>
</div>
