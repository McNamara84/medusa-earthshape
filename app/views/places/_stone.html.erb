	  <div >
            <%= form_tag link_by_global_id_place_stones_path(@place),method: :post do %>
		    <%= hidden_tabname_tag(__FILE__) %>
		      <div style="float:left; margin:5x">
				<%= label_tag :global_id, "Link Sample to this Location", class: "small" %>	      
				<div class="input-group">
				<%= select_tag :global_id, options_for_select(Stone.choose_global_id(@current_user)), { include_blank: true  , id: "stone-global-id", class: "form-control input-sm"} %>
				<span class="input-group-addon">
				<%= link_to stones_path(per_page: 10, format: :modal), "data-toggle" => "modal", "data-target" => "#search-modal", "data-input" => "#stone-global-id" do %>
				  <span class="glyphicon glyphicon-search"></span>
				  <% end %>
				</span>
			      </div>
		    </div>
		     <div style="float:left; margin-top:20px; margin-left:5px">
					  <div>	     
						      <%= button_tag title: "link sample by global id", class: "btn btn-default" do %>
							<span class="glyphicon glyphicon-save"></span>
						      <% end %>
					  </div>
			      
		    </div>
          <% end %>
	  </div>

<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th></th>
      <th>name</th>
 <!--     <th>physical form</th>
 -->     <th>quantity</th>
      <th>unit</th>
      <th></th>
 <% if @current_user.advanced then %>  
      <th>global-id</th>
 <% end %>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= form_tag bundle_edit_stones_path, id: "stone-bundle-form" do %>
      <% @place.stones.each do |stone| %>
        <tr>
          <td>
            <%= check_box_tag("ids[]", stone.id, false, {id: "ids_#{stone.id}", class: "select-row"}) if can?(:update, stone) %>
          </td>
          <td><%= link_to_if can?(:read, stone), stone.name,stone %></td>
 <!--         <td><%= stone.physical_form.try!(:name) %></td>
-->          <td><%= stone.try!(:quantity) %></td>
          <td><%= stone.try!(:quantity_unit) %></td>
          <td></td>
 <% if @current_user.advanced then %>  
          <td><%= stone.try!(:global_id) %></td> 
<% end %>
	<td>
            <%= link_to place_stone_path(@place,stone) + tab_param(__FILE__), method: :delete ,title: "unlink sample",data: {confirm: t("confirm.unlink")} do %>
              <span class="glyphicon glyphicon-remove"></span>
            <% end %>
          </td>
        </tr>
      <% end %>
    <% end %>
  </tbody>
  <tfoot>

        <tr>
            <%= hidden_tabname_tag(__FILE__) %>
            <th colspan="3"><%= check_box_tag nil, nil, false, class: "select-row-all" %> Select all</th><th></th><th></th><th></th><th></th>
        </tr>
<tr><th colspan="3"> <%= link_to_function "bundle edit", "$('#stone-bundle-form').submit();", class: "btn btn-default" %><th><th></th><th></th><th></th></tr>


  </tfoot>
</table>



<% if @current_user.advanced then %>

 <div >
      <%= form_for Stone.new, url: place_stones_path(@place) do |f|%>
           <%= hidden_tabname_tag(__FILE__) %>
		<%= render partial: "stones_new", locals: { stone: Stone.new, f: f, group_id: @place.group_id} %>
		<div style="float:left; margin:5px">

			  <div class="form-group form-group-sm">

				  <div style="margin-top:20px;">
				      <%= f.button title:"add sample", class: "btn btn-default" do %> 
					<span class="glyphicon glyphicon-save"></span>
				      <% end %>
				  </div>
			</div>
			
		</div>

      <% end %>
      
  </div>

<% end %>	


