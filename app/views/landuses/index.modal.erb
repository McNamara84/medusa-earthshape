<%= paginate @landuses, remote: true %>
<%= search_form_for @search, url: polymorphic_path(@search.klass, format: :modal), remote: true do |f| %>
<table class="table table-condensed table-striped">
  <thead>
    <tr>
      <th>
        <%= f.button class: "btn btn-default" do %>
        <span class="glyphicon glyphicon-refresh"></span>
        <% end %>
      </th>      
      <th><div><%= sort_link @search, :name, Landuse.model_name.human, { format: :modal }, remote: true %></div></th>
     
    </tr>
    <tr>
      <th><%= text_field_tag :per_page, (params[:per_page] || Stone.default_per_page), size: 4, readonly: true %></th>
      <th><div><%= f.text_field :name_cont, size: 15 %></div></th>
    </tr>
    <tr>
      <th></th>
      <th></th>
    </tr>
  </thead>
  <tbody>
    <%= render partial: "landuse.modal", collection: @landuses, locals: { checked: false } %>
  </tbody>
</table>
<% end %>

