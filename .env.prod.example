# PostgreSQL Configuration
POSTGRES_USER=medusa
POSTGRES_PASSWORD=changeme_secure_password
POSTGRES_DB=medusa_production

# pgAdmin Configuration
PGADMIN_EMAIL=admin@example.com
PGADMIN_PASSWORD=changeme_pgadmin_password

# Traefik Configuration
TRAEFIK_HOST=env.rz-vm182.gfz.de

# Rails Configuration
# Generate SECRET_KEY_BASE with: docker compose -f docker-compose.prod.yml run --rm web bundle exec rake secret
SECRET_KEY_BASE=changeme_generate_with_rake_secret

# Optional: Custom Rails settings
# RAILS_MAX_THREADS=5
# WEB_CONCURRENCY=2
